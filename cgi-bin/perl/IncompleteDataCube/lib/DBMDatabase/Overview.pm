package DBMDatabase::Overview;

=pod

=head2 Description

This is a simple database to store objects.
That class provides methods to swizzle and unswizzle objects.

The DBMDatabase uses DBM files to store the objects.
There is one DBM file for each L<DBMDatabase::Table>.

=cut

1;
